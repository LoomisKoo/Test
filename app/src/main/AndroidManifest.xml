<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.test">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/noAnimTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />

        <activity android:name=".activity.SecondActivityBase" />
        <activity android:name=".activity.TabActivity" />
        <activity android:name=".activity.WebActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.LoginPlayAndroidActivity" />
        <activity android:name=".activity.KnowledgeSystemArticleActivity" />
        <activity android:name=".activity.LottieActivity" />
        <activity android:name=".activity.CollectionArticleActivity" />
        <activity
            android:name=".activity.MovieDetailActivity"
            android:theme="@style/TransitionsTheme" />
        <activity
            android:name=".activity.BigImgActivity"
            android:theme="@style/TransitionsTheme" />
        <activity
            android:name=".activity.ViewBigImageActivity"
            android:theme="@style/TransitionsTheme" />
        <activity android:name=".activity.ProjectHomeActivity" />
    </application>

</manifest>